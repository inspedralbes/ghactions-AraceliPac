name: SSH Connection Test

on: [push]

jobs:
  ssh-test:
    runs-on: ubuntu-latest
    steps:
      - name: Provar connexió SSH
        run: ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ${{ secrets.PROD_USER }}@${{ secrets.SERVER_IP }} "echo 'Connexió exitosa!'"
